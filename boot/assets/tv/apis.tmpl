{{define "apis"}}
<!DOCTYPE html>
<html lang="en" class="color-toggle-light" color-mode="light">
{{template "head"}}

<body>
{{template "svg-sprite"}}

<div class="wrapper dark-mode-dim">
  {{template "header"}}
  <main class="container flex flex-even">
    {{template "aside"}}
    <!--- main page --->
    <div class="gdoc-page">
      <!--- header nav --->
      <div class="gdoc-page__header flex flex-wrap justify-between" itemprop="breadcrumb">
        <div>
          <svg class="icon gdoc_path hidden-mobile"><use xlink:href="#gdoc_path"></use></svg>
          <ol class="breadcrumb">
            <li><a href="/v1/rk/tv/application"><span>Application</span></a></li>
            <li>/</li>
            <li><span itemprop="name">API</span></li>
          </ol>
        </div>
      </div>

      <!--- content --->
      <article class="gdoc-markdown">
        <!--- title --->
        <div class="gdoc-page__anchorwrap">
          <h1 id="api"> API </h1>
          </div>
          <!--- content --->
          <table>
            <thead>
              <tr>
                <th>Entry Name</th>
                <th>Method</th>
                <th>Path</th>
                <th>Port</th>
                <th>Swagger URL</th>
              </tr>
            </thead>
            <tbody>
              {{ range .Entries }}
              <tr>
                <td>{{ .EntryName }}</td>
                <td>{{ .Rest.Method }}</td>
                <td>{{ .Rest.Pattern }}</td>
                <td>{{ .Rest.Port }}</td>
                <td><a target="_blank" href="{{ .Rest.SwUrl }}">{{ .Rest.SwUrl }}</a></td>
              </tr>
              {{ end }}
            </tbody>
          </table>
        </div>
      </article>
    </div>
  </main>
  {template "footer"}}
</div>
</body>
</html>
{{end}}